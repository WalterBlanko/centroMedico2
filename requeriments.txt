npm i express && npm i body-parser && npm i cors && npm i oracledb

Para activar host de la BD activar:
node serve.js <- se encuentra en ./centroMedico2

Para conectar NodeJS con Angular hay que crear un nuevo servicio
ng g s services/database

Nota: hay que tener ejecutando el "node serve.js" al mismo tiempo que "ng serve" para que funcione